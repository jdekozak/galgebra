#if !defined(GALGEBRA_MAP_ITERATOR_FWD)
#define GALGEBRA_MAP_ITERATOR_FWD

namespace galgebra
{
    struct map_iterator_tag;

    template<typename Sequence, int Position>
    struct map_iterator;
}

#endif
